# Elder Abuse AI — Backend Dependencies
# Python 3.10+ required (tested on 3.14)

# ── FastAPI core ───────────────────────────────────────────────
fastapi
uvicorn[standard]
python-multipart
pydantic

# ── Environment ────────────────────────────────────────────────
python-dotenv

# ── Audio Processing ───────────────────────────────────────────
pydub
audioop-lts          # required for pydub on Python 3.13+
soundfile
numpy

# ── Speech-to-Text (Groq Whisper API) — optional voice input ───
groq

# ── Text-to-Speech — Bangla questions/results read aloud ───────
edge-tts           # bn-BD neural voices, free, no API key

# ── PDF Generation (Phase 3) ───────────────────────────────────
fpdf2
uharfbuzz          # HarfBuzz text shaping — required for correct Bengali conjuncts

# ── Gemini LLM — RAG engine + entity extractor (Phase 3) ───────
google-generativeai

# ── Firestore — anonymized dashboard records (saved only on consent)
# Falls back to a local JSON file if backend/firebase-key.json is absent.
firebase-admin

# ── Optional: vector_store.py (thesis comparison only, NOT in main
#    pipeline). Heavy (~1GB torch). Install only if running the
#    embedding/ChromaDB comparison. Main RAG uses Gemini, not these.
# chromadb
# sentence-transformers
# langchain
# langchain-community

# ── Testing ────────────────────────────────────────────────────
pytest
httpx

# ── Data ───────────────────────────────────────────────────────
pandas
